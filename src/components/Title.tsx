// Components/ui
import { Box } from '@mui/material';

const Title = () => {
  return (
    <Box id="title">
      <span>FRESNEL ZONE </span>
      <br />
      <span>CALCULATOR </span>
    </Box>
  );
};

export default Title;
