// Components/ui
import { Box } from '@mui/material';

const Title = () => {
  return (
    <Box id="title">
      <span>ZONA FRESNEL </span>
      <br />
      <span>CALCULADORA </span>
    </Box>
  );
};

export default Title;
